<?php

defined('MOODLE_INTERNAL') || die();

$plugin->version   = 2015120700;               // If version == 0 then module will not be installed
$plugin->requires  = 2012120300;      // Requires this Moodle version
$plugin->maturity  = MATURITY_ALPHA;
$plugin->component = 'local_strathjiscla'; // To check on upgrade, that module sits in correct place